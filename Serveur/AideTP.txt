// Pour compiler
javac *a 

// Pour créer une archive
mkdir META-INF
jar cvf Exo1.jar *s META-INF

// Regarder dans une archive sans l'extraire
jar tvf Exo1.jar

// Démarrage asadmin
asadmin start-domain

// Compiler Client :
 javac -classpath Exo2.jar Client.java
java -cp "%CLASSPATH%:=;Exo2.jar" --add-opens java.base/java.lang=ALL-UNNAMED Client